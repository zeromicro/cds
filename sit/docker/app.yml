version: '2.4'
# networks:
#   bridge:
#     external: true
services:
  dm:
    image: cds
    container_name: dm
    volumes:
      - ../../dm/cmd/sync/etc/dm.json:/cds/dm/cmd/sync/etc/dm.json
    command:
       /cds/build/dm -f /cds/dm/cmd/sync/etc/dm.json
  galaxy:
    image: cds
    container_name: galaxy
    restart: always
    ports:
      - "8080:8080" # for debuging
    volumes:
      - ../../galaxy/etc/galaxy-api.json:/cds/galaxy/etc/galaxy-api.json
    working_dir: /cds/galaxy
    command:
      /cds/build/galaxy -f /cds/galaxy/etc/galaxy-api.json
  rtu:
    image: cds
    container_name: rtu
    volumes:
      - ../../rtu/conf.json:/cds/rtu/conf.json
    command:
      /cds/build/rtu -f /cds/rtu/conf.json
  nginx:
    image: nginx
    container_name: nginx
    restart: always
    ports:
      - 3414:80
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./nginx/:/var/log/nginx
      - ../../web/static:/usr/local/html
    depends_on:
      - galaxy
      - rtu
      - dm 